import Foundation

public struct NthPrimeState {
    public let prime: Int
    public let result: Int
    
    public init(prime: Int, result: Int) {
        self.prime = prime
        self.result = result
    }
}
